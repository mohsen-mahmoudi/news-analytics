spring:
  cloud:
    config:
      fail-fast: true
      name: news-to-kafka
      profile: kafkaToElastic
      username: spring_cloud_user
      password: spring@123!@#

  config:
    import: configserver:http://localhost:8888
